#include <stdio.h>
#include <math.h>

int main(){

    double A=0, B=0, C=0, R1=0, R2=0;

    scanf("%lf %lf %lf", &A, &B, &C);

    //R1 = ((-(B) - (sqrt(C)))/(2*A);
    //R2 = ((-(B) + (sqrt(C)))/(2*A);

    printf("R1 = %.5lf\nR2 = %.5lf\n", R1, R2);

    return 0;
}