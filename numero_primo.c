#include <stdio.h>

int main(){

    int i=0, j=0 qtd=0, num;

    scanf("%d", &qtd);

    while (i < qtd)
    {
        scanf("%d", &num);
        i++;

        for(j=0; j<num; j++){
            if()
        }
    }
    

    return 0;
}